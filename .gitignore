# ============================================================
# Materiales crudos — nunca al repo.
# Los masters (video, fotos HD, fuentes sin subsetear) viven acá
# mientras se procesan. Solo el resultado final optimizado entra
# a src/assets/. Ver HERO_SPEC.md: "hero-master.mp4 ... dejarlo
# fuera del repo, Git no es para binarios de 15 MB".
# ============================================================
/assets/
/fonts/

# ============================================================
# Node / Vite
# ============================================================
node_modules/
dist/
.vite/
*.local

# ============================================================
# Secretos
# ============================================================
.env
.env.*
!.env.example

# ============================================================
# Sistema / editor
# ============================================================
.DS_Store
*.log

# ============================================================
# Transcripciones de sesión — contexto de trabajo, no código.
# Se conservan localmente pero no entran al repo: el historial de
# lo decidido vive en los mensajes de commit y en los MD de la raíz.
# ============================================================
session-*.txt

# ============================================================
# Documentación interna y material del cliente — no se publica.
# El repo es público para que Javier pueda revisar la URL, así que
# acá queda fuera todo lo que no es el sitio:
#   · el manual de marca es propiedad del cliente
#   · PLAN.md lleva estimaciones y riesgos del encargo
#   · CLAUDE.md y HERO_SPEC.md son doctrina de trabajo interna
#   · legacy/ y spike-fase0/ son material de referencia ya consumido
# Todo sigue en el disco de Charlie. Cuando el repo migre a la cuenta
# de la marca, en privado, estos archivos vuelven: son parte del
# handoff de la Fase 7.
# ============================================================
Brand-Guidelines-NUDE.pdf
CLAUDE.md
PLAN.md
HERO_SPEC.md
/legacy/
/spike-fase0/
/skills/
